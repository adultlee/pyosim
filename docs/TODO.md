# TODO

## 메인 UI 리디자인 (기획 확정 2026-06-17)

기획·디자인 확정됨 → `docs/main-ui-plan.md`, 스펙 `docs/twin-vote-spec.md`. 아래는 구현만 남음.

- [ ] **정당색 유틸** (`web/src/partyColor.ts`) — 정당명 → 계열색 매핑 + 회색 폴백 (색표: twin-vote-spec.md §3)
- [ ] **순위 쌍 필터 드롭다운화** — 칩 7줄(1·2위~91·92위) → "순위 ▾" 하나로 (가장 큰 시각 개선)
- [ ] **그룹 카드 + 3컬럼 그리드** — TwinVoteViewer 그룹 렌더를 둥근 카드 + 투표소 3열 그리드로. 앞 6칸 + "나머지 N곳 더보기"
- [ ] **배지 위계** — 반복 횟수만 강조 배지(민트, 많을수록 진하게). 투표구분·정당은 낮춤(모두 배지 금지)
- [ ] **App.tsx 인트로** — 정의 문구를 새 기준으로 교체("서로 다른 두 읍면동에서 같은 두 후보 같은 표"). 배경 박스·송도 수치는 유지
- [ ] **types.ts** — 정당·투표수 옵셔널 필드 추가 (하위호환: 있으면 표시, 없으면 생략)
- [ ] 기본 정렬 = 득표 큰 순, 보조 = 반복 횟수 순 토글

UI는 데이터와 독립 — 지금 구현·커밋 가능. 정당·투표수 실제 값 채우기는 아래 데이터 작업에 의존.

## 데이터 재생성 (별도 — 입력 CSV가 다른 데스크탑)

- [ ] `analyze_twin_votes.py`에 **각 후보 10표 미만 동률 제외** 필터 추가 (현재 0표만 제거됨)
- [ ] **합계(`계`) 행 입력 제외** 확인
- [ ] **최소 단위 읍면동 통일** — 3회 등 투표구(`제N투`/`투표소`)를 읍면동으로 합산
- [ ] 후보 **정당** + **투표수/선거인수** 필드를 CSV→JSON에 보존 (카드 배지·규모용)
- [ ] 검증된 CSV로 JSON 재생성 → `web/public/twin_votes_*.json` 갱신
