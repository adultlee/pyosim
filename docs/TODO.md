# TODO

## 메인 UI 리디자인 (기획 확정 2026-06-17)

기획·디자인 확정됨 → `docs/main-ui-plan.md`, 스펙 `docs/twin-vote-spec.md`.

### 1차 구현 완료 (2026-06-17)
- [x] **정당색 유틸** (`web/src/partyColor.ts`) — 정당명 → 계열색 매핑 + 회색 폴백
- [x] **순위 쌍 필터 드롭다운화** — 칩 7줄 → "순위 ▾" select
- [x] **그룹 카드 + 3컬럼 그리드** — 둥근 카드 + 투표소 3열 + "나머지 N곳 더보기"
- [x] **배지 위계** — 반복 횟수만 강조 배지(민트, 많을수록 진하게)
- [x] **App.tsx 인트로** — 정의 문구 새 기준으로 교체. 배경 박스·송도 수치 유지
- [x] **types.ts** — 정당·투표수 옵셔널 필드 추가 (하위호환)
- [x] 기본 정렬 = 득표 큰 순, 보조 = 반복 횟수 순 토글

### 메타데이터 / 공유
- [x] 브라우저 탭 제목·lang·description·favicon·OG 텍스트 태그 (`web/index.html`, Vite 기본값 → 표심)
- [ ] **OG 이미지 제작** — `web/public/og-image.png` (1200×630). 공유 미리보기용. 현재 og:image 태그는 있으나 파일 없음 → 카톡/트위터 공유 시 이미지 안 뜸. HTML→PNG 캡처 또는 직접 디자인

### 2차 보강 (색·데이터·레이아웃 — 화면이 심심/비어 보임)
- [ ] **시각 보강 (데이터 불필요)** — 1·2위 후보 색 구분, 카드 좌측 강조 바(반복 횟수 색), 동률 숫자 강조, 카드 헤더 틴트. 정당 데이터 없어도 화면을 화려하게
- [ ] **카드 1열 비어 보임 해소** — 아래 "레이아웃 미해결" 참고. 방향 정한 뒤 구현
- [ ] **정당색 실제 적용 (데이터 필요)** — JSON `parties`(후보→정당) 채워지면 색 점/배지 자동 표시. 데이터 재생성에 의존
- [ ] **투표 규모 표시 (데이터 필요)** — 각 투표소 `투표수`/`선거인수` 채워지면 칸에 "투표 N중" 표시

## 레이아웃 미해결 — 카드 1열이 비어 보임 (2026-06-17)

카드가 세로 1열이라 가로로 넓은데 내용(동률 2명 + 투표소 몇 칸)이 적어 비어 보임.
여러 열로 깔면 번잡. 후보 방향(택1 필요):
- 카드 폭을 콘텐츠에 맞게 좁히고 가운데 정렬(max-width)로 여백 줄이기
- 1열 유지하되 카드 안을 더 채우기(규모·미니 막대그래프·지도 등 — 단 데이터 필요)
- 반복 적은 카드(2~3곳)는 컴팩트 행, 반복 많은 카드만 큰 카드로 — 밀도 차등
- 좌: 동률 요약 / 우: 투표소 그리드 2분할 가로 레이아웃

## 데이터 재생성 (별도 — 입력 CSV가 다른 데스크탑)

- [ ] `analyze_twin_votes.py`에 **각 후보 10표 미만 동률 제외** 필터 추가 (현재 0표만 제거됨)
- [ ] **합계(`계`) 행 입력 제외** 확인
- [ ] **최소 단위 읍면동 통일** — 3회 등 투표구(`제N투`/`투표소`)를 읍면동으로 합산
- [ ] 후보 **정당** + **투표수/선거인수** 필드를 CSV→JSON에 보존 (카드 배지·규모용)
- [ ] 검증된 CSV로 JSON 재생성 → `web/public/twin_votes_*.json` 갱신
